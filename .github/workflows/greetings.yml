name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: 'Great to see this here, thanks for your first contribution'
        pr-message: 'Awesome, thanks for your help. Now get a cup of coffee your code we´ll be reviewed'
